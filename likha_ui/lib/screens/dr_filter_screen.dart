import 'package:flutter/material.dart';

class drFilterScreen extends StatelessWidget {
  const drFilterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SingleChildScrollView(
        child: Column(
          children: [

          ],
        ),
      ),
    );
  }
}
